# ვარაუდები და მიღებული გადაწყვეტილებები

ტექნიკურ დავალებაში ორაზროვან ადგილებზე არჩეულია ინდუსტრიის სტანდარტული პრაქტიკა.
ქვემოთ ჩამოთვლილია ყველა ასეთი გადაწყვეტილება და მისი მიზეზი.

> **გზების წაკითხვა:** §1–§7 ფარდობითია `frontend/`-ის მიმართ, §8 — `backend/`-ის.
> §9 ორივეს ეხება.

---

## 1. მონაცემები და კონტრაქტი

**1.1. სურათების ფორმატი — `.svg`, არა `.jpg`.**
დავალების მაგალითში ეძლევა `/images/products/ph-001-1.jpg`. რეალური ფოტოები არ
გვაქვს, `.jpg`-ზე მითითება კი ყოველ სურათზე 404-ს გამოიწვევდა (ხმაური console-ში).
ამიტომ `scripts/gen-images.mjs` აგენერირებს **183 SVG-ს** (61 პროდუქტი × 3), თითოს
კატეგორიის შესაბამისი გრაფიკითა და პროდუქტის id-დან ნაწარმოები უნიკალური ფერით.
ველის *ფორმა* კონტრაქტის იდენტურია — იცვლება მხოლოდ გაფართოება. `ProductImage`-ს
`onError` fallback-ი `placeholder.svg`-ზე ისევ აქვს.

**1.2. პროდუქტების რაოდენობა — 61 (მინიმუმი 60).**
15 ტელეფონი, 12 კაბელი, 10 power bank, 8 დამტენი, 10 ყურსასმენი, 6 აქსესუარი.

**1.3. `specs`-ის boolean მნიშვნელობები.**
`fastCharge`, `wireless`, `anc` ინახება როგორც `true/false`, არა როგორც სტრიქონი —
toggle-ფილტრებისთვის ეს ბუნებრივია. UI-ში ისინი ითარგმნება „დიახ/არა“-დ
(`formatSpecValue`). `specs.network` სტრიქონია (`'5G'` / `'4G'`), ამიტომ მისი
toggle-ფილტრი იყენებს `match: '5G'`-ს.

**1.4. `createdAt`.**
mock-ში იწერება მოკლედ (`created: '2026-07-14'`) და `make()`-ში გადაიქცევა სრულ
ISO 8601 სტრიქონად. `isNew` პროდუქტებს მიენიჭათ ბოლო თვეების თარიღები, რომ
„სიახლით“ სორტი ლოგიკური იყოს.

**1.5. `import`-ებში გაფართოებები აშკარაა** (`../utils/format.js`).
Vite ორივეს იტანს, მაგრამ აშკარა გაფართოებით მოდულები პირდაპირ Node-შიც იტვირთება —
ეს გამოყენებულია `scripts/gen-images.mjs`-ში.

---

## 2. Data layer

**2.1. `getProductById` და `getOrderByNumber` დამატებულია** კონტრაქტში.
პირველი — შიდა საჭიროებისთვის, მეორე — `/checkout/success/:id` გვერდისთვის.
ორივეს აქვს `httpApi`-ის შესაბამისი ჩონჩხი.

**2.2. Facet-ების დათვლის წესი.**
თითოეული ჯგუფის ოფციები ითვლება **ყველა სხვა ფილტრის** გათვალისწინებით, თავად ამ
ჯგუფის გამოკლებით. ეს სტანდარტული e-commerce ქცევაა: ერთი ჯგუფის შიგნით ოფციები
არჩევისას არ ქრება. 0-იანი ოფციები ჩანს, მაგრამ disabled-ია.

**2.3. `getRelatedProducts` — ±30% ფასის დიაპაზონი „რბილია“.**
თუ დიაპაზონში საკმარისი პროდუქტი არ არის, სია ივსება იმავე კატეგორიის სხვა
პროდუქტებით ფასის სიახლოვის მიხედვით — რომ სექცია არასდროს იყოს ცარიელი.

**2.4. სორტირებისას მარაგში არმყოფი პროდუქტები ყოველთვის ბოლოშია**, არჩეული
სორტის მიუხედავად.

**2.5. „პოპულარობა“ = `rating × log10(reviewsCount + 10)`.**
სუფთა რეიტინგით სორტი ერთ-შეფასებიან პროდუქტს წინ წამოსწევდა.

**2.6. Guest-ის შეკვეთები ჩანს ავტორიზაციის შემდეგაც.**
`getOrders()` აბრუნებს მიმდინარე მომხმარებლის შეკვეთებს **პლუს** ამ ბრაუზერში
სტუმრად გაფორმებულებს (`userId === null`) — რეალურ backend-ზე ეს წესი შეიცვლება.

---

## 3. ძებნა

**3.1. მოკლე და რიცხვითი ტოკენები მხოლოდ ზუსტად ემთხვევა.**
თუ „2“ პრეფიქსით ემთხვეოდა, `USB C 2m` დააბრუნებდა კაბელს გარანტიით „24 თვე“;
თუ „m“ პრეფიქსით ემთხვეოდა — Power Bank-ს „mAh“-ის გამო. ამიტომ ≤2 სიმბოლოიან და
წმინდა რიცხვით ტოკენებზე მოქმედებს მკაცრი, სიტყვასთან ზუსტი დამთხვევა.

**3.2. ქართული სტემინგი უხეშია, განზრახ.**
დაბოლოებების სია (`ები`, `ის`, `ში`, `ზე`, `ით`, `ს`, `ი`…) ჭრება მხოლოდ მაშინ,
როცა ფუძეს ≥4 სიმბოლო რჩება. ეს საკმარისია `ყურსასმენი ↔ ყურსასმენები` ტიპის
შემთხვევებისთვის; სრული მორფოლოგიური ანალიზი ბიბლიოთეკას მოითხოვდა.

**3.3. ათასეულების შერწყმა მუშაობს ველის შიგნით.**
`20 000 mAh` → `20000 mah`, მაგრამ `iPhone 15` + `128 GB` (სხვადასხვა ველი ან
ასოთი მოსდევს) არ ერწყმის — ამიტომ ნორმალიზაცია ხდება ველ-ველზე, გაერთიანებამდე.

**3.4. კატეგორიის წონა — 5.**
დავალებაში კატეგორიის წონა მითითებული არ იყო, მაგრამ ქართული სახელით ძებნა
სავალდებულოა (`უსადენო ყურსასმენი`). მოთავსდა `brand`-სა და `specs`-ს შორის.

**3.5. ძებნის შედეგებში ნაგულისხმევი სორტი — რელევანტურობა.**
სხვა სორტის არჩევისას იგი URL-ში ჩაიწერება; კატალოგში default რჩება „პოპულარობით“.

---

## 4. UI / UX

**4.1. ძებნის შედეგებზე ფილტრებად კატეგორია და ბრენდი გამოიტანება.**
შედეგები კატეგორიათაშორისია, ამიტომ `specs.*` ფილტრები აზრს კარგავს.
იგივე გენერიკული `FilterSidebar` გამოიყენება, უბრალოდ სხვა კონფიგით.

**4.2. მთავარი გვერდის სექციები ლიმიტირებულია 8 პროდუქტით.**
Mobile-ზე ჰორიზონტალური carousel (snap-scroll), desktop-ზე 4-სვეტიანი grid.

**4.3. „კალათაში დამატება“ ბარათიდან ამატებს 1 ცალს** და გვერდს არ ცვლის;
toast-ს აქვს ღილაკი „კალათის ნახვა“. ბარათი უკვე დამატებულ პროდუქტზე აჩვენებს
„კალათაშია (N)“.

**4.4. Undo მხოლოდ კალათის გვერდზეა.**
`Cart`-ში წაშლის toast-ს აქვს „დაბრუნება“; სხვაგან წაშლა არ ხდება.

**4.5. მიწოდება: 0 ₾ თუ subtotal ≥ 150 ₾, სხვა შემთხვევაში 5 ₾.**
ციფრები ცხოვრობს `constants/index.js`-ის `SHIPPING`-ში, ლოგიკა —
`utils/pricing.js`-ში. კომპონენტებში hardcoded ციფრი არ არის.

**4.6. Checkout-ზე ონლაინ გადახდა არ არის** (Non-goal). არჩევანია
„ნაღდი მიღებისას“ და „ბარათით კურიერთან“.

**4.7. შეკვეთის ნომრის ფორმატი — `VB-YYMM-XXXX`** (მაგ. `VB-2609-4821`).

**4.8. ტელეფონის ველი ფორმატირდება აკრეფისას** (`555 12 34 56`), backend-ს კი
გადაეცემა მხოლოდ ციფრები. `+995` პრეფიქსი ავტომატურად ჩამოიჭრება.

**4.9. Mobile-ზე ფილტრები bottom-sheet-ია**, desktop-ზე — sticky sidebar.
Sheet-ის footer-ში ჩანს „ჩვენება (N)“ ნაპოვნის რაოდენობით.

**4.10. `document.title` ფორმატია `<გვერდი> · VoltBox`** (`useDocumentTitle`).

---

## 5. არქიტექტურა

**5.1. დამატებული ფაილები დავალების სტრუქტურასთან შედარებით.**
`utils/pricing.js` (მიწოდება/ჯამები — `format.js`-ს არ ეკუთვნის),
`services/errors.js` (შეცდომების იერარქია, საერთო mock/http-ისთვის),
`constants/index.js`, `types.js`, `components/common/{Badge,ProductImage,CategoryIcon,Textarea,ErrorBoundary}.jsx`,
`components/layout/{Logo,UserMenu,RequireAuth,ScrollToTop}.jsx`,
`components/product/ProductCarousel.jsx`, `scripts/gen-images.mjs`.

**5.2. `hooks/useCart.js`, `useAuth.js`, `useToast.js` — თხელი re-export-ებია**
შესაბამისი Context-იდან. ასე იმპორტი კომპონენტებში ერთგვაროვანია
(`hooks/`-იდან), ხოლო provider-ები `context/`-ში რჩება.

**5.3. კატეგორიები იტვირთება ერთხელ.**
`useCategories()` ინახავს promise-ს მოდულის დონეზე — Header, Footer, Category და
SearchResults ერთსა და იმავე მოთხოვნას იზიარებენ. ჩატვირთვა მაინც `api.js`-ზე გადის.

**5.4. React Router-ის `future` ალმები ჩართულია.**
`v7_startTransition`, `v7_relativeSplatPath` და დანარჩენები — რომ dev-console
სუფთა იყოს deprecation warning-ებისგან.

**5.5. `import.meta.env` წაკითხვა დაცულია `?.`-ით**, რომ მოდულები Vite-ის გარეშეც
იტვირთებოდეს (მაგ. სურათების გენერატორის სკრიპტში).

**5.6. i18n ბიბლიოთეკა არ გამოიყენება** (Non-goal), მაგრამ ყველა განმეორებადი
ტექსტი თავმოყრილია `constants/index.js`-ში — მოგვიანებით ის ერთ ლექსიკონად
გადაიქცევა კომპონენტების შეხების გარეშე.

**5.7. ტესტების ფრეიმვორკი არ დამატებულა** (Non-goal). ვერიფიკაცია ჩატარდა
დროებითი jsdom smoke/flow სკრიპტებით (ყველა მარშრუტი, სრული შესყიდვის გზა,
კალათის ჰიდრაცია, ავტორიზაცია), რომლებიც შემდეგ წაიშალა — repo-ში `devDependency`
არ დარჩენილა.

---

## 6. Bundle და data layer (შემდგომი ოპტიმიზაცია)

**6.1. `virtual:api-impl` alias `if`-ის ნაცვლად.**
თავდაპირველად `api.js` ორივე იმპლემენტაციას სტატიკურად აიმპორტებდა და არჩევანს
runtime-ზე აკეთებდა. გაზომვამ აჩვენა, რომ `VITE_API_MODE=http`-ის შემთხვევაშიც
ბილდი **ბაიტში ბაიტამდე იგივე რჩებოდა** — 61-პროდუქტიანი mock ბაზა პროდაქშენში
ხვდებოდა. მიზეზი: Rollup `data/products.js`-ის მოდულის დონეზე `.map()`-ს
პოტენციურ side effect-ად თვლის და ვერ აგდებს.

მხოლოდ ლიტერალის დაკეცვა (`.toLowerCase()`-ის მოცილება) საკმარისი არ აღმოჩნდა —
`httpApi` ამოვარდა, `mockApi` კი დარჩა. ამიტომ არჩევანი გადავიდა **ბილდის
კონფიგში**: `vite.config.js` `resolve.alias`-ით ერთადერთ ფაილს ხსნის და
გამოუყენებელი იმპლემენტაცია მოდულების გრაფში საერთოდ არ ხვდება.

ფასი: `virtual:api-impl` სუფთა Node-ით არ იხსნება (ტესტ-სკრიპტებში იგივე alias
ხელით უნდა გადაეცეს esbuild-ს). სარგებელი: შედეგი დეტერმინისტულია და არა
tree-shaking-ის ევრისტიკებზე დამოკიდებული.

**6.2. Layout, Header და Footer განზრახ არ არის lazy.**
ისინი ყველა მარშრუტზე საჭიროა — გაყოფა მხოლოდ დამატებით HTTP მოთხოვნებს
დაამატებდა და პირველ რენდერს გააჭიანურებდა.

**6.3. `<Suspense>` ერთია, Layout-ის დონეზე.**
თითოეული მარშრუტის ცალკე შეხვევა უფრო მოქნილია, მაგრამ 15-ჯერ გამეორებულ კოდს
მოითხოვდა. საერთო fallback (`PageFallback`) კატალოგის იმავე გეომეტრიას იმეორებს,
ამიტომ გადასვლისას layout არ ხტება.

**6.4. Prefetch on hover არ დამატებულა.**
გვერდის chunk-ები 1–11 KB-ია (gzip) და ლოკალურ ქსელზე მყისიერად იტვირთება.
ჰოვერზე წინასწარი ჩატვირთვა აზრს იძენს მხოლოდ მაშინ, თუ chunk-ები გაიზრდება.

---

## 7. მკვდარი კოდის დახურვა

**7.1. `formatReviews` / `formatItemsCount` ჩაერთო.**
რაოდენობის ტექსტი („N პროდუქტი“) ხელით იყო აწყობილი Category-სა და
SearchResults-ში — ეს §10-ს ეწინააღმდეგებოდა („რაოდენობების ფორმატირება —
მხოლოდ `utils/format.js`-ით“). სიტყვა „ნაპოვნია“ გადავიდა `TEXT`-ში.
`RatingStars`-ს დაემატა `showReviewsLabel` პროპი: ბარათებზე რჩება კომპაქტური
`(128)`, პროდუქტის გვერდზე კი იწერება სრული „128 შეფასება“.

**7.2. `useLocalStorage` და `STORAGE_KEYS.recentSearches` გაცოცხლდა.**
ორივე დავალების სტრუქტურით იყო მოთხოვნილი, მაგრამ მომხმარებელი არ ჰყავდა.
ახლა ორივეს იყენებს `hooks/useRecentSearches.js` — SearchBar ინახავს ბოლო 5
ძებნას და ცარიელ ველზე ფოკუსისას აჩვენებს.

Header SearchBar-ს **ორჯერ** ირენდერებს (desktop და mobile), ამიტომ `remember`
და `forget` ჩაწერამდე localStorage-ს ხელახლა კითხულობენ, ხოლო `sync()`
ფოკუსზე ეშვება — თორემ უხილავი ეგზემპლარის მოძველებული state გადააწერდა.

**7.3. `data/brands.js` ჩაერთო არქიტექტურის დაცვით.**
UI-ს `data/`-ის იმპორტი ეკრძალება, ამიტომ ბრენდის ცნობარს **data layer**
იყენებს: `mockApi.decorate()` პროდუქტს ამატებს `brandCountry`-ს, რომელიც
პროდუქტის „მახასიათებლების“ ცხრილში ჩანს. დაემატა `getBrands()` კონტრაქტში
(mock + httpApi ჩონჩხი).

ბრენდის ლოგოების იდეა უარყოფილია: რეალური ლოგოები არ გვაქვს, გარე სერვისზე
დამოკიდებულება აკრძალულია, ასო-ინიციალი კი სახელის გვერდით არაფერს მატებს.

---

## 8. Backend (FastAPI + PostgreSQL)

**8.1. სტეკიდან სამი გადახვევა.**
`Python 3.12` → **3.14** (მომხმარებლის გადაწყვეტილება; `cp314` wheel-ები ყველა
პაკეტისთვის დადასტურდა). `python-jose` → **PyJWT** (jose 2021-იდან მიტოვებულია,
აქვს CVE-2024-33663/33664). `passlib[argon2]` → **argon2-cffi** პირდაპირ
(passlib 1.7.4-ია 2020-იდან და Python 3.13+-ზე `crypt`-ის მოცილებით ტყდება).

**8.2. სქემას 7 სვეტი დაემატა დავალებასთან შედარებით.**
დავალების §1 ამბობს „the frontend wins“. `categories.filters` და `.icon`,
`products.short_description`, `.is_new`, `.reviews_count`, `brands.country`,
`users.first_name/last_name` — ამათ გარეშე UI ნაწილობრივ არ დაიხატებოდა.
`low_stock_threshold` არის 3 და არა 5 — frontend-ის `LOW_STOCK_THRESHOLD`.

**8.3. facets-ის ფორმა დავალებას არ ემთხვევა.**
§7 მასივებს ითხოვდა (`[{slug,name,count}]`), `FilterSidebar.jsx` კი ობიექტ-რუკას
კითხულობს: `facets.values["specs.ram"] → {"8 GB": 5}`. frontend-ს მივყევით.

**8.4. Query-პარამეტრები ორივე ფორმით მიიღება.**
URL-ი მოკლე სახელს აზიარებს (`ram=8+GB`), `httpApi.js`-ის სერიალიზატორი კი
სრულს (`specs.ram=8 GB`). ფასიც ორივენაირად: `1000-3000` და `1000,3000`.
ერთის მხარდაჭერა ფილტრს ჩუმად დაკარგავდა — ეს ბაგი Phase 7-ში დაიჭირა.

**8.5. `GET /orders/{number}` კონტაქტს ითხოვს სტუმრისგან.**
ნომერი თანმიმდევრობითია (`VB-YYYYMMDD-NNNN`) და გამოცნობადი. `httpApi.js`
შეკვეთის გაფორმებისას ტელეფონს `guest-orders:v1`-ში ინახავს და დეტალების
მოთხოვნისას თან აყოლებს. ეს frontend-ის ერთადერთი ქცევითი ცვლილებაა.

**8.6. `createOrder` `snapshot`-ს აღარ აგზავნის.**
`ApiRequest`-ს `extra="forbid"` აქვს: ფასის შემცველი ველი 400-ია. კალათის
snapshot ლოკალური ქეშია და სერვერს არ სჭირდება — ფასს ის ბაზიდან იღებს.

**8.7. RLS ჩართულია, მაგრამ ავტორიზაციის მექანიზმი არაა.**
ცხრილებზე RLS პოლიტიკის გარეშეა (deny-all). backend service-role-ით უერთდება
და მას გვერდს უვლის; ownership-შემოწმებები სერვისების ფენაშია. RLS აქ
უსაფრთხოების ბადეა შემთხვევითი პირდაპირი წვდომისგან.

**8.8. `search_text` აპლიკაციამ უნდა შეავსოს.**
GENERATED სვეტად არ გამოდის — შიგთავსი `brands`-სა და `categories`-ზეც არის
დამოკიდებული, Postgres-ის generated column კი მხოლოდ იმავე row-ს ხედავს.
ივსება seed-შიც და ტესტ-ფაბრიკაშიც, რომ ტესტები რეალურ write-გზას იმეორებდნენ.

**8.9. დეველოპმენტი ლოკალურ Postgres-ზე, არა Supabase-ზე.**
`docker compose up -d db` — მიგრაციები დღეში ათჯერ იშლება და იწერება, ტესტები
ცხრილებს ცლიან. Supabase-ზე გადასვლა მხოლოდ `DATABASE_URL`-ის ცვლილებაა;
`config.py` სქემასა და `?sslmode=`-ს თავად ასწორებს.

**8.10. ruff/mypy-ის target 3.13-ია, თუმცა `requires-python` 3.14.**
`target-version = "py314"` სცადა და უარვყავი: ruff format მაშინ
`except (ValueError, TypeError):`-ს PEP 758-ის `except ValueError, TypeError:`-ად
გადაწერს სამ ფაილში. სინტაქსი 3.14-ზე ვალიდურია, მაგრამ Python 2-ის
`except E, name:`-ს ჰგავს (სადაც `name` ცვლადია, არა მეორე ტიპი) — კითხვადობის
რეალური ზარალი ნულოვანი მოგებისთვის. იმავე target-ზე UP037 SQLAlchemy-ის
`Mapped["Product"]` forward reference-ებსაც ბრჭყალებს ხდის და mapping-ს PEP 649-ის
დაგვიანებულ გამოთვლაზე დაამოკიდებდა. უფრო დაბალი target ამოწმებს ყველაფერს, რასაც
მაღალი — უბრალოდ ახალ იდიომებს არ თავაზობს.

---

## 9. Repo-ს განლაგება

**9.1. `frontend/` + `backend/` ერთ repo-ში, ერთ დონეზე.**
თავიდან frontend repo-ს ძირში იდო (`src/`, `public/`, `package.json`…), backend
კი `backend/`-ში — ორი თანაბარმნიშვნელოვანი ნაწილი სხვადასხვა დონეზე. სამი
რეალური ხარჯი ჰქონდა: (1) ორი `.env` სხვადასხვა მნიშვნელობით და მხოლოდ ერთი
მათგანი საიდუმლოთი, (2) ძირში `npm install`-ის გაშვება Python-ის მხარესაც
ეჩვენებოდა როგორც „პროექტის ძირი“, (3) deploy-ის base directory-ს მითითება
შეუძლებელი იყო frontend-ისთვის ცალკე.

ცალკე repo-ები არ აირჩა: კონტრაქტი (`services/api.js` ↔ Pydantic სქემები) და
mock მონაცემები (`scripts/export_mock_data.mjs` პირდაპირ კითხულობს
`frontend/src/data/*.js`-ს) ერთ commit-ში უნდა იცვლებოდეს — გაყოფა ვერსიების
სინქრონიზაციის ამოცანას წარმოშობდა უსარგებლოდ.

**9.2. root-ში `package.json` არ დაიდო.**
სცდუნებელი იყო `"dev": "npm --prefix frontend run dev"`, მაგრამ ეს ორ
`package.json`-ს ქმნის ერთ ხეში: ძირში `npm install` არაფერს დააინსტალირებდა,
`npm audit` და IDE-ს TypeScript სერვისი კი ორივეს ეცდებოდა. npm workspaces
გადაწყვეტდა, თუმცა hoisting-ს და `node_modules`-ის ერთ საერთო ხეს გულისხმობს —
ეს backend-იან monorepo-ში მოგებას არ იძლევა. `cd frontend` აშკარაა და არაფერს
მალავს.

**9.3. `vite.config.js` `process.cwd()`-ს აღარ იყენებს.**
`loadEnv`-ს envDir-ად კონფიგის საკუთარი დირექტორია გადაეცემა
(`new URL('.', import.meta.url)`). `process.cwd()` სწორი მხოლოდ მაშინაა, თუ
ბრძანება `frontend/`-იდან გაეშვა; ძირიდან `vite --config frontend/vite.config.js`
`.env`-ს ვერ იპოვიდა და `VITE_API_MODE=http` ჩუმად `mock`-ად წაიკითხებოდა — ანუ
შეცდომა არ იქნებოდა, უბრალოდ არასწორი ბანდლი აეწყობოდა.

**9.4. CI ორ workflow-ად დაიყო, `paths:` ფილტრით.**
`frontend.yml` და `backend.yml` მხოლოდ მაშინ ეშვება, როცა შესაბამისი საქაღალდე
შეიცვალა. frontend-ის ბილდი matrix-ით ორივე რეჟიმზე გადის (`mock` და `http`) —
`virtual:api-impl` alias ბილდის დროს იხსნება, ამიტომ ერთ რეჟიმში გატეხილი
იმპორტი მეორის ბილდში საერთოდ არ ჩანს.
